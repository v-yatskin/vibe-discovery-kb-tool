filters:
  and:
    - type == "initiative"
    - or:
        - status == "planned"
        - status == "in_progress"
views:
  - type: table
    name: Active initiatives
    groupBy:
      property: status
      direction: DESC
    order:
      - file.name
      - title
      - status
      - priority
      - linked_problems
