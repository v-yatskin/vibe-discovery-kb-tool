filters:
  and:
    - 'file.inFolder("00_Drafts")'
views:
  - type: table
    name: "My drafts (pending publish)"
    order:
      - file.name
      - note.type
      - note.title
      - file.mtime
    groupBy:
      property: file.mtime
      direction: DESC
